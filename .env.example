# Copy to `.env` and fill in real values. `.env` is gitignored.

# Claude (Anthropic) - required for article rewriter / chat
ANTHROPIC_API_KEY=

# OpenAI - required for FCAT embeddings
OPENAI_API_KEY=

# MongoDB Atlas connection string - required for /api/save-to-fcat
MONGODB_URI=
